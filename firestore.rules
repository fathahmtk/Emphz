
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for all content
    match /{document=**} {
      allow read: if true;
      allow write: if false; // Disallow all writes from the client
    }
  }
}
